{
  "name": "kata",
  "version": "1.0.0",
  "description": "Kata",
  "main": " ",
  "directories": {
    "lib": "src",
    "test": "test"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "ts-jest --watchAll --verbose --coverage --updateSnapshot --notify",
    "coverage": "ts-jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.*",
    "sinon": "^9.2.4",
    "ts-jest": "^27.0.7"
  },
  "dependencies": {
    "typescript": "^4.4.4"
  }
}
